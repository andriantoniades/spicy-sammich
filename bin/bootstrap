#! /bin/bash

# link important rc files to $HOME so that "shit just works"
ln -s ~/.config/bash_profile ~/.bash_profile
ln -s ~/.config/bashrc ~/.bashrc
ln -s ~/.config/vim ~/.vim
ln -s ~/.config/vimrc ~/.vimrc
